import { ConfirmationContent } from '@components'

export default {
  title: 'Support/ConfirmationContent',
  component: ConfirmationContent,
}

export const Default = {
  render: (props) => <ConfirmationContent {...props} />,
}
